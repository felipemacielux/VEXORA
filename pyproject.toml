[build-system]
requires = ["setuptools", "wheel"]

[project]
name = "vexora"
version = "0.1.0"
dependencies = [
    "Django>=4.2",
    "psycopg2-binary",
    "python-decouple",
]

[tool.setuptools.packages.find]
where = ["src"]